# ── Auth mode ───────────────────────────────────────────────
# Leave OIDC_ISSUER_URL blank (or remove it) to run without authentication.
#   docker compose up              → no auth
#   docker compose --profile auth up  → with oauth2-proxy + Synology SSO
#
# Synology SSO Server OIDC configuration
# Get client ID/secret from DSM > SSO Server > OIDC > Applications
OIDC_ISSUER_URL=https://your-nas.example.com/webman/sso
OIDC_CLIENT_ID=your-application-id
OIDC_CLIENT_SECRET=your-application-secret

# External URLs
EXTERNAL_URL=https://webfrac.example.com
FRACTAL_PUBLIC_URL=https://fractal.example.com

# oauth2-proxy cookie (only needed with --profile auth)
# Generate with: python3 -c "import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
OAUTH2_PROXY_COOKIE_SECRET=generate-a-32-byte-base64-secret
COOKIE_DOMAIN=.example.com

# Optional: restrict Fractal MCP to a specific audience claim
# OIDC_AUDIENCE=fractal-mcp

# Existing settings
GIT_USER_NAME=Armen Nakashian
GIT_USER_EMAIL=anakashian@gmail.com
PROJECTS_PATH=/path/to/your/projects

# Port mappings
HTTPS_PORT=4180
HOST_PORT=3011
